import React from "react";

function LogARun(props) {
  return (
    <div className="logARun">
      log a run page

      <button onClick={props.LogARunButton}>Log A Run</button>
    </div>
  );
      
}

export default (LogARun);
